interface:
  display_name: "Claude Code Orchestrator"
  short_description: "Route Claude Code worker roles through CCSwitch while Codex stays in control."
  default_prompt: "Use $claude-code-orchestrator to plan or run Claude Code worker roles through CCSwitch, with Codex as controller."
